use strategy_api::{Bar, Context, OnBar, StateBlob, export_strategy};

pub struct {{strategy_name}} {
    // add your state fields here
}

impl OnBar for {{strategy_name}} {
    fn new() -> Self {
        Self {
            // initialize state
        }
    }

    fn name() -> &'static str {
        "{{display_name}}"
    }

    fn description() -> &'static str {
        "{{description}}"
    }

    fn version() -> &'static str {
        "{{version}}"
    }

    fn restore_state(&mut self, state: StateBlob) {
    }

    fn init(&mut self, total_bars: usize) {
        // reset state before each backtest run
    }

    fn on_bar(&mut self, bar: &Bar, index: usize, ctx: &mut Context) {
        // implement your strategy logic here
        // submit orders:   ctx.submit_order(Side::Long, OrderKind::Market, qty);
        // mark signals:    ctx.mark_buy(bar.close);  ctx.mark_sell(bar.close);
    }

    fn on_finish(&mut self, ctx: &mut Context) -> Vec<u8> {
        // optional: final processing after all bars
        vec![]
    }
}

export_strategy!({{strategy_name}});
